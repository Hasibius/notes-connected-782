<%= link_to "Unfollow", delete_follow_path(user), method: :delete, class: 'secondary-button d-inline-flex',
    data: {action: 'click->follow#destroyFollow',
                  follow_target: 'destroy',
                  user_id: user.id } %>
